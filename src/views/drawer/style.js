import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  view: {
    padding: 20,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: 200,
    marginBottom: -40,
  },
  img: {
    marginTop: 50,
    width: '100%',
    resizeMode: 'stretch',
    height: '60%',
  },
})
