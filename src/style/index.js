import { StyleSheet } from 'react-native'

export const globalStyles = StyleSheet.create({
  view: {
    padding: 30,
    flex: 1,
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
})
