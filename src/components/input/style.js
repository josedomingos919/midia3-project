import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  input: {
    width: '100%',
    minHeight: 24,
    padding: 10,
  },
  vwinput: {
    paddingLeft: 20,
    alignItems: 'center',
    backgroundColor: 'white',
    borderRadius: 20,
    display: 'flex',
    flexDirection: 'row',
    width: '100%',
    marginBottom: 20,
  },
})
